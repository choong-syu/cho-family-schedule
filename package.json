{
  "name": "cho-family-schedule",
  "version": "0.1.0",
  "private": true,
  "description": "조씨네 가족 일정 관리 프로토타입",
  "scripts": {
    "start": "node outputs/server.js",
    "start:file": "node outputs/server.js",
    "start:sqlite": "set STORAGE_DRIVER=sqlite&& node outputs/server.js",
    "migrate:sqlite": "node outputs/migrate-sqlite.js",
    "hash:password": "node outputs/hash-password.js"
  }
}
